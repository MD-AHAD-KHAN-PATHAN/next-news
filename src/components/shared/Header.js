import { Box, Container } from "@mui/material";

const Header = () => {
    return (
        <Box>
            <Container>
                
            </Container>
        </Box>
    );
};

export default Header;